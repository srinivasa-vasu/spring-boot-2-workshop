---
apiVersion: v1
kind: Service
metadata:
  name: cn-config-service
  labels:
    app: cn-config-service
spec:
  selector:
    app: cn-config-service
  ports:
    - name: web
      port: 8080
      targetPort: 8080
