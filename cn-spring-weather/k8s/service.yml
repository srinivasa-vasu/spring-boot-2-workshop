---
apiVersion: v1
kind: Service
metadata:
  name: cn-weather-service
  labels:
    app: cn-weather-service
spec:
  selector:
    app: cn-weather-service
  ports:
    - name: web
      port: 80
      targetPort: 8080
